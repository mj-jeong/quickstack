node_modules/
dist/
*.tsbuildinfo
.env
.env.local
coverage/
.DS_Store

# claude
.claude/

# project docs & task definitions (not shipped)
# NOTE: if these get re-tracked, run: git rm -r --cached docs/ tasks/ scripts/ prompts/
docs/
tasks/
scripts/

# requirement prompts (root only, NOT src/prompts/)
/prompts/
